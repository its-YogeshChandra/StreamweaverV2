[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-multipart = "0.7.2"
actix-web = "4.13.0"
serde = {version = "1.0.228", features = ["derive"]}
tokio = {version = "1.50.0", features= ["full"]}

